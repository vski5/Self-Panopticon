import { IConfig } from "./interfaces";
import "./styles/index.scss";
export declare class Widget {
    private _config;
    private _eventBus;
    private _el;
    private _widgetElem;
    private isVisible;
    private _onMessageFromWidget;
    private loaderElem;
    private assetsLoaded;
    private isAppLoaded;
    constructor(config: IConfig);
    assetsLoader(): void;
    loadAssets(srcList: string[]): Promise<any[]>;
    getAssets(): Promise<any>;
    create(): Promise<void>;
    show(): void;
    hide(): void;
    withdrawExit(payload: any): void;
    on(event: string, callback: Function): this;
    off(event: string, callback: Function): void;
    isMobileDevice(): boolean;
    destroy(): void;
    static assetsUrl: string;
}
